# jaysfoundry-blog

## What This Is
The Jay's Foundry blog — a personal site for writing about building with AI tools, product management meets hands-on development, and learning in public. Live at jaysfoundry.com.

- **Brand:** Jay's Foundry / "Build things. Learn in public. Stay curious."
- **Stack:** Astro 6, MDX, strict TypeScript, deployed on Vercel
- All global `~/.claude/CLAUDE.md` and `~/shop/CLAUDE.md` preferences apply to this project

## Commands
- `npm run dev` — dev server at localhost:4321
- `npm run build` — production build to `./dist/`
- `npm run preview` — preview production build locally
- `npx astro check` — TypeScript type checking

## Architecture

**Content layer:** Blog posts live in `src/content/blog/` as `.md`/`.mdx` files. The collection schema is defined in `src/content.config.ts` — frontmatter requires `title`, `description`, `pubDate`, and optionally `updatedDate` and `heroImage` (validated with Zod).

**Routing:** File-based routing in `src/pages/`. Dynamic blog post pages use `src/pages/blog/[...slug].astro` with `getStaticPaths()` + `getCollection('blog')`.

**Layouts:** `src/layouts/BlogPost.astro` wraps individual posts. Components in `src/components/` include `BaseHead.astro` (SEO/meta tags), `Header.astro`, `Footer.astro`, `FormattedDate.astro`, and `HeaderLink.astro`.

**Site constants:** `src/consts.ts` holds `SITE_TITLE` and `SITE_DESCRIPTION`.

**Config:** `astro.config.mjs` — integrations: MDX, Sitemap. Site URL: `https://jaysfoundry.com`.

**Styles:** Global CSS in `src/styles/global.css`.

**Static assets:** `public/` for files served as-is; `src/assets/` for images processed by Astro's image pipeline (sharp).

## Project Structure
```
src/
  content/blog/    # Markdown posts (generated by publish pipeline or added directly)
  pages/           # Astro pages
  layouts/         # Page layouts
  components/      # Astro components
  styles/          # Global CSS
  consts.ts        # Site title, description
  content.config.ts # Collection schema (Zod validation)
scripts/
  publish.mjs      # Notion → markdown sync script
.github/
  workflows/
    publish.yml    # Manual trigger publish pipeline
```

## Publish Pipeline
Content is authored in Notion (Learning Log → Blog Post entries → Status = Published). The pipeline syncs Notion to the repo — Code does not author blog content.

1. `scripts/publish.mjs` queries Notion API for published Blog Post entries
2. Converts Notion blocks to Astro-compatible markdown with frontmatter
3. Writes to `src/content/blog/`, cleans up orphaned files
4. Skips unchanged files via SHA-256 hash
5. GitHub Action commits changes and pushes to `main`
6. Vercel auto-deploys

**To trigger:** `gh workflow run publish` from CLI, or GitHub Actions UI → `publish` workflow → Run workflow.

**Secrets** (GitHub repo secrets): `NOTION_API_TOKEN`, `NOTION_DATABASE_ID`

**Do not manually edit pipeline-managed files in `src/content/blog/`** — they'll be overwritten on next publish run. Direct markdown files (not from Notion) are fine if they don't conflict with Notion-generated slugs.

## Conventions
- This is a **public repo** — recruiter test applies to all committed files
- Astro TypeScript strict mode — respect existing tsconfig
- Content authoring happens in Notion and Claude Chat (Blog & Content project), not in Code. Code handles site features, theme, pages, components, and images.
- See global CLAUDE.md for commit style and general conventions

## Environment Variables
Secrets are in `.env` for local dev. See `.env.example` for required keys. Never commit secrets.

## Future Work
- Preview deploys (PR-based branch workflow with Vercel preview URLs)
- Project showcase pages
- Image support in blog posts
- Theme refinements