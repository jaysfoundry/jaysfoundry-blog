---
interface Props {
  date: Date;
}

const { date } = Astro.props;
const month = String(date.getMonth() + 1).padStart(2, '0');
const day = String(date.getDate()).padStart(2, '0');
const short = `${month}.${day}`;
---

<time datetime={date.toISOString()}>{short}</time>
